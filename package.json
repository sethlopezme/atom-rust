{
  "name": "atom-rust",
  "main": "./lib/atom-rust",
  "version": "0.0.1",
  "description": "Provide Rust language support via the Rust Language Server",
  "keywords": [
    "rust",
    "language",
    "server"
  ],
  "repository": "https://github.com/sethlopezme/atom-rust",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
    "atom-languageclient": "git://github.com/atom/atom-languageclient.git"
  },
  "consumedServices": {
    "status-bar": {
      "versions": {
        "^1.0.0": "consumeStatusBar"
      }
    }
  },
  "configSchema": {
    "command": {
      "title": "RLS Command",
      "description": "Command to start the Rust Language Server.",
      "type": "string",
      "default": "rustup run nightly rls"
    },
    "dev": {
      "title": "RLS Development Mode",
      "description": "Build and run RLS via Cargo. Requires the `RLS_ROOT` environment variable. Overrides \"RLS Command\" setting.",
      "type": "boolean",
      "default": false
    }
  }
}
